# Leave unset to use the relative "/api" path (works with the Vite dev proxy in vite.config.js,
# or when the backend is served from the same origin as this frontend in production).
# Set to an absolute URL (e.g. https://api.example.com/api) if the backend is on a different host.
VITE_API_BASE_URL=
